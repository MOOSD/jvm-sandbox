<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>aio</id>
    <baseDirectory>/</baseDirectory>

    <!-- 指定打包格式为jar -->
    <formats>
        <format>jar</format>
    </formats>
    <fileSets>

        <!-- jar中添加配置文件 -->
        <fileSet>
            <directory>${project.build.outputDirectory}</directory>
            <outputDirectory/>
            <includes>
                <include>sandbox.properties</include>
                <include>sandbox-logback.xml</include>
            </includes>
        </fileSet>

        <!-- 添加core -->
        <fileSet>
            <directory>${core.temp.path}</directory>
            <outputDirectory>core</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
        <!-- 添加spy -->
        <fileSet>
            <directory>${spy.temp.path}</directory>
            <outputDirectory>spy</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>

        <!-- 添加module文件 -->
        <fileSet>
            <directory>${modules.temp.path}</directory>
            <outputDirectory>modules</outputDirectory>
            <excludes>
                <exclude>original-*.jar</exclude>
            </excludes>
            <includes>
                <include>**/*.jar</include>
            </includes>
        </fileSet>


    </fileSets>
    <dependencySets>
        <!-- 以agent依赖为基础 -->
        <dependencySet>
            <outputDirectory/>
            <includes>
                <include>com.alibaba.jvm.sandbox:sandbox-agent</include>
            </includes>
            <unpack>true</unpack>
        </dependencySet>


    </dependencySets>


</assembly>
