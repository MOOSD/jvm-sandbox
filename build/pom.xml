<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.alibaba.jvm.sandbox</groupId>
        <artifactId>sandbox</artifactId>
        <version>1.4.0</version>
    </parent>
    <groupId>org.example</groupId>
    <artifactId>build</artifactId>
    <name>build</name>
    <packaging>jar</packaging>


    <dependencies>
        <!-- 使用如下依赖方式来控制打包时的顺序 -->
        <dependency>
            <groupId>com.alibaba.jvm.sandbox</groupId>
            <artifactId>sandbox-agent</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.jvm.sandbox</groupId>
            <artifactId>sandbox-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.jvm.sandbox</groupId>
            <artifactId>sandbox-spy</artifactId>
        </dependency>
    </dependencies>

    <properties>
        <modules.temp.path>${project.build.directory}/module</modules.temp.path>
        <core.temp.path>${project.build.directory}/core</core.temp.path>
        <spy.temp.path>${project.build.directory}/spy</spy.temp.path>
        <agent.finalName>hawkeye-agent-${project.version}</agent.finalName>
    </properties>

    <build>
        <plugins>
            <!-- 取消主jar包的生成 -->
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-jar</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
            <!-- 汇总所有的module -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${modules.temp.path}"/>
                                <copy todir="${modules.temp.path}" flatten="true">
                                    <fileset dir="../sandbox-module-starter" includes="**/*.jar"/>
                                </copy>
                                <mkdir dir="${core.temp.path}"/>
                                <copy todir="${core.temp.path}" flatten="true">
                                    <fileset dir="../sandbox-core" includes="**/sandbox-core*.jar"/>
                                </copy>

                                <mkdir dir="${spy.temp.path}"/>
                                <copy todir="${spy.temp.path}" flatten="true">
                                    <fileset dir="../sandbox-spy" includes="**/sandbox-spy*.jar"/>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- 指定打包的manifest文件信息 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>attached</goal>
                        </goals>
                        <configuration>
                            <finalName>${agent.finalName}</finalName>
                            <descriptors>
                                <descriptor>src/assembly/descriptor.xml</descriptor>
                            </descriptors>
                            <archive>
                                <manifestEntries>
                                    <Premain-Class>com.alibaba.jvm.sandbox.agent.AgentLauncher</Premain-Class>
                                    <Agent-Class>com.alibaba.jvm.sandbox.agent.AgentLauncher</Agent-Class>
                                    <Can-Redefine-Classes>true</Can-Redefine-Classes>
                                    <Can-Retransform-Classes>true</Can-Retransform-Classes>
                                    <Can-Set-Native-Method-Prefix>true</Can-Set-Native-Method-Prefix>
                                </manifestEntries>
                            </archive>
                            <!-- 取消打包时候对模块的压缩 -->
                            <archiverConfig>
                                <compress>false</compress>
                            </archiverConfig>

                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
